<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity"
    android:background="@color/colorDarkGrey">

    <Button
        android:id="@+id/firstButton"
        android:layout_width="300dp"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:layout_centerHorizontal="true"
        android:layout_above="@+id/secondButton"
        android:layout_marginBottom="50dp"
        android:background="@color/colorWhite"
        android:text="Вывести информацию о студентах" />

    <Button
        android:id="@+id/secondButton"
        android:layout_width="300dp"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:layout_centerHorizontal="true"
        android:layout_centerVertical="true"
        android:background="@color/colorWhite"
        android:text="Внести запись в таблицу" />

    <Button
        android:id="@+id/thirdButton"
        android:layout_width="300dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="50dp"
        android:layout_centerHorizontal="true"
        android:layout_below="@+id/secondButton"
        android:background="@color/colorWhite"
        android:gravity="center"
        android:text="Заменить ФИО в последней записи
        на Иванов Иван Иванович" />




</RelativeLayout>